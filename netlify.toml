[build]
  publish = "."

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[functions."poll-scores"]
  schedule = "* * * * *"

[functions."send-reminders"]
  schedule = "0 * * * *"
